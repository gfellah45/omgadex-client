import React, { FC } from "react";

const Dashboard: FC = () => {
  return (
    <div className="flex items-center justify-center flex-1 h-screen">
      <div className="text-3xl">
        hello this is the dashoard we can start proper work now
      </div>
    </div>
  );
};

export default Dashboard;
